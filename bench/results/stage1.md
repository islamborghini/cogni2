# Stage 1 — cAST retrieval baseline

_Generated by the gated eval; run it to populate real numbers:_

```sh
export VOYAGE_API_KEY=…
export COGNI_EVAL=1
export COGNI_BENCH_REPO=/path/to/django   # checked out at the target_sha in bench/tasks.yaml
go test -tags eval ./bench/ -run Retrieval -v -timeout 30m
```

This writes the headline `recall@10` (localization, macro-averaged) and
`mean_retrieved_code` tokens here, plus a per-task table. Until then this file is
a placeholder.
